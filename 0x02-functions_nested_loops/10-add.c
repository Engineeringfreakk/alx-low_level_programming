#include "main.h"
/**
 * add - A function that adds two integers and returns the result
 * @x: function parameter one
 * @y: function parameter two
 * Return: a + b
 */
int add(int x, int y)
{
	return (x + y);
}
