#include <stdio.h>
/**
 * main - Program that prints the lowercase alphabet in reverse
 * Return: 0 (Success)
 */
int main(void)
{
	char x;

	x = 'z';
	while
		(x >= 'a')  {
			putchar(x);
			x--;
		}
	putchar('\n');
	return (0);
}
